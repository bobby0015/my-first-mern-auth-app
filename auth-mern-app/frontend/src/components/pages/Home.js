import React from 'react'
import { ToastContainer } from 'react-toastify'

const Home = (handleReceiveFunction) => {


  return (
    <div>
      <button>Go</button>
    </div>
  )
}

export default Home
